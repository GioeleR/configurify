# Manichinofy con React+Redux

To install:

            npm install
            jspm install

To build the project:

            gulp build

To run the app simply run

            gulp serve
and visite http://localhost:8080